<div class="d-flex h-100">
  <Sidebar
          @service={{model.service}}
          @step3={{true}}
  />
  <div class="col-9 bg-light p-4 h-100">
    <div class="d-flex flex-wrap">
      {{#each model.offices as |office|}}
        <OfficeCard
                @office={{office}}
        />
      {{/each}}
    </div>
  </div>
</div>

